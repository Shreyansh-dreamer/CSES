#include<iostream>
#include<conio>
#include<vector>
#include<cstring>
#include<math>
#include<algorithm>

int memo[1000001][2];

int main(){
    int t,n;
    cin>>t;
    while(t--){
        cin>>n;
        memset(memo,-1,sizeof(memo));
        cout<<dp(n,n,)
    }
}